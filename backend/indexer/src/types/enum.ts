export enum TxnSubValue {
  Full = "full",
  Signatures = "signatures",
  Accounts = "accounts",
  None = "none",
}

export enum IndexType {
  Block = "block",
  Txn = "txn",
  Account = "account",
  Program = "program",
  Slot = "slot",
  Log = "log",
}
